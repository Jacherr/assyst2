use twilight_model::gateway::payload::incoming::GuildCreate;

pub fn handle(event: GuildCreate) {
    // check if we should handle this guild create based on what the cache returns
    // check assyst 1 source
}
